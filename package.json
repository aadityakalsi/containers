{
  "name": "containers",
  "version": "1.0.0",
  "description": "A containers library for JavaScript",
  "main": "index.js",
  "scripts": {
    "test": "mocha tests --timeout 10000 --slow 0ms",
    "cov": "nyc --reporter=lcov mocha tests --timeout 10000 --slow 0ms",
    "perf-rec": "env NODE_ENV=production mocha tests --timeout 10000 --prof --slow=0ms",
    "perf-rep": "node --prof-process isolate-*-v8.log > perf-results.txt && rm *.log"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "chai": "^4.1.2",
    "cpuprofilify": "^1.0.0",
    "mocha": "^3.5.3",
    "nyc": "^11.2.1",
    "v8-profiler": "^5.7.0"
  }
}
